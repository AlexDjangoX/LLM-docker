# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test CORS settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Test rate limiting (higher for tests)
RATE_LIMIT_MAX=1000

# Test JWT secrets (never use in production!)
JWT_SECRET=test-jwt-secret-for-testing-only-do-not-use-in-production
JWT_REFRESH_SECRET=test-refresh-secret-for-testing-only-do-not-use-in-production

# Mock service URLs for testing
OLLAMA_BASE_URL=http://mock-ollama:11434
STABLE_DIFFUSION_URL=http://mock-stable-diffusion:7860
XTTS_URL=http://mock-xtts:80
LIBRETRANSLATE_URL=http://mock-libretranslate:5000